# Project conventions

## Workflow

- After completing work on a feature branch and pushing, automatically open a PR into `main` and merge it (squash merge). Don't ask for confirmation each time — the user has standing approval to merge everything.
- Use the GitHub MCP tools for PR creation and merging (no `gh` CLI available).
